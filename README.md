# Fantomex

Minimal self-hosted experiment and run tracking platform. AI-first via MCP.

## Quick start

```bash
uv venv
uv pip install -e ".[dev]"
uv run python -m fantomex.server
```

## Dev server

```bash
./start_dev.sh
```
